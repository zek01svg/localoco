export function LandingPage() {
  return (
    <div className="bg-background relative flex min-h-screen flex-col items-center justify-center p-6">
      <main className="relative z-10 w-full max-w-md space-y-4 text-center">
        <h1 className="text-4xl font-bold tracking-tight">LocaLoco</h1>
        <p className="text-muted-foreground text-lg leading-relaxed font-light">
          Discover and support independent local businesses in Singapore.
        </p>
      </main>
    </div>
  );
}
