{
  "name": "rewritten_project",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "tsc & nodemon -r ./dist/loadEnv.js dist/index.js",
    "build": "tsc",
    "start": "tsc && node -r ./dist/loadEnv.js dist/index.js",
    "auth:generate": "dotenv -e ../.env -- npx @better-auth/cli generate --output src/database/auth-schema.ts --config src/lib/auth.ts",
    "db:pull": "dotenv -e ../.env -- drizzle-kit pull",
    "db:push": "dotenv -e ../.env -- drizzle-kit push",
    "db:generate": "dotenv -e ../.env -- drizzle-kit generate",
    "db:view": "dotenv -e ../.env -- drizzle-kit studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}